// examples/demo.ltl — runnable demo for ltl_pem_read

import std::io
use ltl_pem_read as lib

fn main() {
    io::println("ltl_pem_read demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}